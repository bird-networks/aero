{
  "name": "aero-monorepo",
  "description": "DO NOT PUBLISH",
  "scripts": {
    "format": "cd aero.sh && npm run format && cd ../aeroSW && npm run format && cd AeroSandbox && npm run format"
  },
  "devDependencies": {
    "biome": "^0.3.3"
  }
}
